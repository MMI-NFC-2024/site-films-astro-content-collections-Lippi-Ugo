---
import type { CollectionEntry } from 'astro:content'

interface Props {
    personne: CollectionEntry<"personnes">
}
const { personne } = Astro.props;
---

<a class="text-slate-900 font-medium" href={`/personnes/${personne.id}`}>{personne.data.nom}</a>