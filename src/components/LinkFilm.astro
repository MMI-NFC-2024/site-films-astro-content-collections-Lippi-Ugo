---
import type { CollectionEntry } from 'astro:content'

interface Props {
    film: CollectionEntry<"films">
}
const { film } = Astro.props;
---

<a class="text-slate-900 font-medium" href={`/films/${film.id}`}>{film.data.titre}</a>